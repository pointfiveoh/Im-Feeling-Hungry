<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".MainActivity"
    >
	
	<!-- This page containing two buttons, I want Food, and jk, not hungry lol -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/iwantfoodButton"
        android:id="@+id/iwantfoodButton"
        android:onClick="mainButtonClicked"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/jkButton"
        android:id="@+id/jkButton"
        android:onClick="mainButtonClicked"
        />
    
</LinearLayout>
